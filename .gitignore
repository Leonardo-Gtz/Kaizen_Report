# Archivos subidos por usuarios (no versionar)
uploads/*
!uploads/.gitkeep

# Excel exportados desde RH (se generan en el servidor, no en git)
exportaciones/*
!exportaciones/.gitkeep

# Respaldo local: legado, pruebas, librerías viejas, huérfanos (no subir a prod)
_respaldo-proyecto/

# Logs locales
logs/*
!logs/.gitkeep
*.log

# Configuración con credenciales de BD
config.php
sync-config.php

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini
